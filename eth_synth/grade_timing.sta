# Setup units to match skywater130
set_cmd_units -time ns -capacitance pF -current mA -voltage V -resistance kOhm -distance um

# Read the skywater130 liberty file
read_liberty ./sky130_fd_sc_hd__tt_025C_1v80.lib

# Read the synthesized netlist from Yosys
read_verilog ./synth_outputs/eth_synth_sky130.v

# Link top module
link_design ethmac

# Define timing constraints for a 100ns (10MHz) clock
create_clock -name core_clock -period 100.0 [get_ports wb_clk_i]

# Apply input/output delays
# (Older OpenSTA builds may not support collection helpers like remove_from_collection.)
set_input_delay 0.0 -clock core_clock [all_inputs]
set_output_delay 0.0 -clock core_clock [all_outputs]

# Report the longest path
report_checks -path_delay max -format end -digits 4 > ./metrics/sta_report.txt
exit
